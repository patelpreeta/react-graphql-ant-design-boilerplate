//! Ant Imports

import Layout from "antd/lib/layout";

const { Header } = Layout;

const AppHeader = () => {
  return <Header className="app-header-wrapper">Header</Header>;
};
export default AppHeader;
